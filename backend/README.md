# Washu Backend Package

This directory defines the Python package for the Washu backend. It is
installed in editable mode during development and CI (`pip install -e backend`)
so that modules under `app/` can be imported without relying on the current
working directory.
